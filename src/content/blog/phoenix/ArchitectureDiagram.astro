<figure class="phoenix-architecture-diagram" aria-label="Phoenix request flow">
    <div class="gateway">Gateway</div>

    <div class="branches" aria-hidden="true">
        <span></span>
        <span></span>
    </div>

    <div class="request-paths">
        <section class="path-card read-path" aria-label="Read RPC path">
            <div class="path-title">Read RPC</div>
            <div class="node">Read layer</div>
            <div class="arrow">↓</div>
            <div class="node database">Database</div>
        </section>

        <section class="path-card write-path" aria-label="Write RPC path">
            <div class="path-title">Write RPC</div>
            <div class="pipeline">
                <div class="node">Write layer</div>
                <div class="arrow">↓</div>
                <div class="node muted">Queue</div>
                <div class="arrow">↓</div>
                <div class="node kafka">Kafka</div>
                <div class="arrow">↓</div>
                <div class="node muted">Queue</div>
                <div class="arrow">↓</div>
                <div class="node">Writer</div>
                <div class="arrow">↓</div>
                <div class="node database">Database</div>
            </div>
        </section>
    </div>

    <figcaption>
        Phoenix request flow: reads hit the database directly, while writes are
        ordered through Kafka before being persisted.
    </figcaption>
</figure>

<style>
    .phoenix-architecture-diagram {
        margin: 2rem 0;
        border: 1px solid var(--border);
        border-radius: 0.5rem;
        background: color-mix(in oklch, var(--background) 92%, var(--muted));
        padding: 1rem;
    }

    .gateway,
    .node {
        display: flex;
        min-height: 2.75rem;
        align-items: center;
        justify-content: center;
        border: 1px solid var(--border);
        border-radius: 0.375rem;
        background: color-mix(in oklch, var(--background) 72%, var(--muted));
        color: var(--foreground);
        font-size: 0.875rem;
        font-weight: 600;
        line-height: 1.15;
        text-align: center;
    }

    .gateway {
        width: min(16rem, 100%);
        margin: 0 auto;
        border-color: color-mix(in oklch, var(--primary) 75%, var(--foreground));
        background: color-mix(in oklch, var(--primary) 18%, var(--background));
    }

    .branches {
        display: grid;
        grid-template-columns: 1fr 1fr;
        width: min(34rem, 88%);
        height: 2rem;
        margin: 0.25rem auto 0;
        border-top: 1px solid var(--border);
    }

    .branches span {
        border-left: 1px solid var(--border);
    }

    .branches span:last-child {
        border-right: 1px solid var(--border);
    }

    .request-paths {
        display: grid;
        grid-template-columns: minmax(0, 0.85fr) minmax(0, 1.15fr);
        gap: 1rem;
    }

    .path-card {
        border: 1px solid color-mix(in oklch, var(--border) 72%, var(--foreground));
        border-radius: 0.5rem;
        background: color-mix(in oklch, var(--background) 86%, var(--muted));
        padding: 0.875rem;
    }

    .path-title {
        margin-bottom: 0.75rem;
        color: var(--muted-foreground);
        font-size: 0.8125rem;
        font-weight: 600;
        text-align: center;
    }

    .pipeline {
        display: grid;
    }

    .arrow {
        color: var(--muted-foreground);
        font-size: 1.25rem;
        line-height: 1.4;
        text-align: center;
    }

    .muted {
        background: color-mix(in oklch, var(--background) 82%, var(--muted));
        color: var(--muted-foreground);
    }

    .kafka {
        border-color: color-mix(in oklch, var(--primary) 75%, var(--foreground));
    }

    .database {
        background: color-mix(in oklch, var(--primary) 18%, var(--background));
    }

    figcaption {
        margin-top: 0.875rem;
        color: var(--muted-foreground);
        font-size: 0.875rem;
        text-align: center;
    }

    @media (max-width: 42rem) {
        .phoenix-architecture-diagram {
            padding: 0.75rem;
        }

        .branches {
            display: none;
        }

        .gateway {
            margin-bottom: 0.75rem;
        }

        .request-paths {
            grid-template-columns: 1fr;
        }
    }
</style>
