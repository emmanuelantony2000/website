<figure class="phoenix-database-diagram" aria-label="Phoenix database layout">
    <div class="cache-flow">
        <div class="node">Read layer</div>
        <div class="arrow">↓</div>
        <div class="node cache">Redis cache</div>
        <div class="cache-miss">cache miss</div>
        <div class="arrow">↓</div>
    </div>

    <section class="postgres" aria-label="Postgres database">
        <div class="postgres-title">Postgres database</div>
        <div class="node identity">node: readable ID → UUID</div>

        <div class="groups">
            <section class="group" aria-label="Account state tables">
                <h4>Account state</h4>
                <div class="table-list">
                    <span>balance</span>
                    <span>nonce</span>
                    <span>storage</span>
                    <span>code</span>
                </div>
            </section>

            <section class="group" aria-label="Chain history tables">
                <h4>Chain history</h4>
                <div class="table-list">
                    <span>blocks</span>
                    <span>transactions</span>
                    <span>receipts</span>
                    <span>event logs</span>
                </div>
            </section>

            <section class="group" aria-label="Node metadata tables">
                <h4>Node metadata</h4>
                <div class="table-list">
                    <span>snapshot</span>
                    <span>websocket filters</span>
                    <span>unlocked accounts</span>
                </div>
            </section>
        </div>
    </section>
</figure>

<style>
    .phoenix-database-diagram {
        margin: 2rem 0;
        border: 1px solid var(--border);
        border-radius: 0.5rem;
        background: color-mix(in oklch, var(--background) 92%, var(--muted));
        padding: 1rem;
    }

    .cache-flow {
        display: grid;
        justify-items: center;
        margin-bottom: 0.25rem;
    }

    .node {
        display: flex;
        min-height: 2.75rem;
        align-items: center;
        justify-content: center;
        border: 1px solid var(--border);
        border-radius: 0.375rem;
        background: color-mix(in oklch, var(--background) 72%, var(--muted));
        color: var(--foreground);
        font-size: 0.875rem;
        font-weight: 600;
        line-height: 1.15;
        padding: 0 0.75rem;
        text-align: center;
    }

    .cache,
    .identity {
        border-color: color-mix(in oklch, var(--primary) 70%, var(--foreground));
        background: color-mix(in oklch, var(--primary) 16%, var(--background));
    }

    .arrow {
        color: var(--muted-foreground);
        font-size: 1.25rem;
        line-height: 1.4;
        text-align: center;
    }

    .cache-miss {
        margin-top: 0.25rem;
        color: var(--muted-foreground);
        font-size: 0.75rem;
        font-weight: 600;
    }

    .postgres {
        border: 1px solid color-mix(in oklch, var(--border) 72%, var(--foreground));
        border-radius: 0.5rem;
        background: color-mix(in oklch, var(--background) 86%, var(--muted));
        padding: 0.875rem;
    }

    .postgres-title {
        margin-bottom: 0.75rem;
        color: var(--foreground);
        font-size: 0.9375rem;
        font-weight: 700;
        text-align: center;
    }

    .identity {
        width: min(22rem, 100%);
        margin: 0 auto 0.875rem;
    }

    .groups {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 0.75rem;
    }

    .group {
        min-width: 0;
        border: 1px solid var(--border);
        border-radius: 0.375rem;
        background: color-mix(in oklch, var(--background) 74%, var(--muted));
        padding: 0.75rem;
    }

    .group h4 {
        margin: 0 0 0.625rem;
        color: var(--foreground);
        font-size: 0.8125rem;
        font-weight: 700;
        text-align: center;
    }

    .table-list {
        display: grid;
        gap: 0.375rem;
    }

    .table-list span {
        border: 1px solid var(--border);
        border-radius: 0.3125rem;
        background: color-mix(in oklch, var(--background) 88%, var(--muted));
        color: var(--muted-foreground);
        font-size: 0.75rem;
        line-height: 1.15;
        padding: 0.5rem 0.375rem;
        text-align: center;
    }

    @media (max-width: 42rem) {
        .phoenix-database-diagram {
            padding: 0.75rem;
        }

        .postgres {
            padding: 0.75rem;
        }

        .groups {
            grid-template-columns: 1fr;
        }
    }
</style>
